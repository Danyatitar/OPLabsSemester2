#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
using namespace std;
string find_id(string,int);
vector <string> find_dups(vector <string>);
vector <int> calc_counts(vector <string>);
vector <string> find_dups(vector <string>);
vector <Identyfikator> sort(vector <Identyfikator>);